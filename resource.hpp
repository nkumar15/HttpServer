


class Resource {

public:
  //Resource(std::string path, /* function ?? */);  

private:
  Resource(const Resource& other) = delete;
  Resource& operator=(const Resource& rhs) = delete;

  //std::map<std::string, std::function ??
};

